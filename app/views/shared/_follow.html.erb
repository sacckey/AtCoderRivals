<%= form_for(current_user.relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, atcoder_user.id %></div>
  <%= f.submit "Follow", class: "btn btn-default" %>
<% end %>