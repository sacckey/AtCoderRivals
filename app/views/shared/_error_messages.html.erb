<% if @atcoder_user.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
    <% errors =  @atcoder_user.errors.full_messages.select {|error| error.include?("Atcoder")} %>
      The form contains <%= pluralize(errors.count, "error") %>.
    </div>
    <ul>
    <% errors.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>